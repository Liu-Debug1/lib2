# 标题层级整理发现

- 目标目录包含 6 篇 Markdown 笔记。
- `C++.md` 已有未提交修改，其他目标笔记中也有用户修改；本次只修改标题行及缺失的 H1。
- 新规则的 H2 编号从每个 H1 下的 `1.` 重新开始，H3/H4 跟随 H2 的数字层级，H5 在每个 H4 下从 `(1)` 重新开始。
- 所有目标笔记均无 H6；`C++.md` 是唯一缺少 H1 的笔记，`C语言基础.md` 有 9 个并列 H1。
