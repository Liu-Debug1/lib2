# Claude Code 使用指南

## 一、应用方式

1. **桌面端应用** — 独立桌面客户端，完整的 UI 体验
2. **网页端应用** — 通过浏览器访问 claude.ai/code
3. **IDE 插件** — VS Code / JetBrains 插件，内嵌在编辑器中
4. **终端（推荐）** — CLI 方式运行，更原生、功能更全

## 二、三种模式

| 模式 | 说明 | 适用场景 |
| --- | --- | --- |
| **对话模式** | 默认模式，一问一答 | 日常编码、调试、探索 |
| **计划模式** | `/plan` — 先设计方案再编码 | 复杂功能、架构决策、多文件改动 |
| **循环模式** | `/loop <时间>` — 定时重复执行 | 监控部署、轮询状态、定期检查 |

> [!NOTE] 切换模式快捷键 `Shift + Tab`

## 三、常用命令

| 命令                | 说明                            |
| ----------------- | ----------------------------- |
| `/help`           | 查看帮助                          |
| `/clear`          | 清除对话历史                        |
| `/model <模型名>`    | 切换模型（如 `deepseek-v4-flash`）   |
| `/config`         | 修改设置（主题、模型等）                  |
| `/cost`           | 查看本次会话的 token 消耗              |
| `/doctor`         | 诊断环境问题                        |
| `/plan`           | 进入计划模式，设计方案后再实施               |
| `/review`         | 审查当前分支的代码变更                   |
| `/loop <时间> <命令>` | 按间隔重复执行（如 `/loop 5m /review`） |
| `/fast`           | 切换到快速模式（更快输出）                 |
| `/init`           | 初始化 CLAUDE.md 项目配置文件          |
| `/compact`        | 主动压缩对话历史，降低 token 消耗          |
| `/context`        | 查看上下文占比、token 占比等             |
| `/resume`         | 恢复历史对话                        |
| `/memory`         | 手动打开 Auto-memory，主动记忆         |
| *Alt+V*           | 粘贴图片                          |
| *鼠标右键*            | 粘贴文本                          |
|                   |                               |

## 四、核心使用技巧

### 1. 善用 CLAUDE.md 项目配置

在项目根目录创建 `CLAUDE.md`，Claude 会自动读取并遵循其中的规则和约定。

> [!caution] `CLAUDE.md` 而非 `.md`

**推荐包含内容：**
- 项目简介和技术栈
- 目录结构说明
- 编码规范与约定
- 常用命令（构建、测试、部署）
- 测试策略

### 2. 提供上下文信息

在提问时，附上相关信息可以提高准确率：
- 文件路径和行号
- 错误日志全文
- 相关代码片段
- 你已尝试过的方案

### 3. 使用计划模式处理复杂任务

对于涉及多文件改动或架构决策的任务：
1. 输入 `/plan` 进入计划模式
2. 让 Claude 分析现有代码并设计方案
3. 审阅方案，确认后再执行
4. 批准后自动进入实施阶段

### 4. 利用工作区隔离（Worktree）

处理多个分支任务时，使用工作区隔离：
- 不同分支在隔离环境中工作
- 不影响主工作区的未提交更改
- 完成后可选择保留或清理

### 5. 记忆系统

Claude Code 会自动记忆关键信息：
- **用户偏好**：角色、风格偏好、知识背景
- **项目上下文**：目标、进度、决策
- **反馈指导**：你纠正过的行为模式

### 6. 权限管理

- 敏感操作（删除、推送、覆盖文件）会请求确认
- 可通过 `/config` 调整权限级别
- 常用只读操作可加入允许列表减少打扰

## 五、效率技巧

| 技巧           | 说明                            |
| ------------ | ----------------------------- |
| **并行任务**     | 可以同时让 Claude 做多个独立任务          |
| **批量编辑**     | 一次描述多个改动，Claude 会逐步处理         |
| **背景运行**     | 长时间任务可在后台运行，不阻塞对话             |
| **/loop 轮询** | 监控部署状态：`/loop 2m gh run view` |
| **快捷确认**     | 输入 `! <命令>` 执行 shell 命令并返回结果  |
| **指定文件**     | 提到文件时用绝对路径，避免歧义               |
| **> 引用**     | 用 `> ` 引用之前的消息，Claude 会优先关注   |

## 六、最佳实践

**避免的用法：**
- ❌ 模糊指令：「优化这段代码」→ 具体说明优化方向
- ❌ 过多无关上下文 → 只提供相关的文件和日志
- ❌ 一次要求太多改动 → 拆分成多个小任务

**推荐的用法：**
- ✅ 先讨论方案再实施（尤其复杂任务）
- ✅ 改动后主动审查 diff
- ✅ 测试覆盖关键路径
- ✅ 记录项目约定到 CLAUDE.md

## 七、模型配置（接入 DeepSeek）

| 配置项         | 填入模型                | 分配逻辑说明                                                               |
| ----------- | ------------------- | -------------------------------------------------------------------- |
| 主模型         | `deepseek-v4-flash` | 日常默认用速度快、成本低的 flash，适合代码补全、简单问答、API 查询等高频轻量任务                        |
| Haiku 默认模型  | `deepseek-v4-flash` | Haiku 本身是 Claude 的轻量化高速模型，完美匹配 flash 定位，做工具调用、文件读取等轻量操作              |
| Sonnet 默认模型 | `deepseek-v4-pro`   | Sonnet 是 Claude 中阶主力模型，对应 Pro 的中高等推理能力，处理常规代码编写、逻辑分析任务               |
| Opus 默认模型   | `deepseek-v4-pro`   | Opus 是 Claude 最高阶模型，对应 Pro 的最强推理能力，处理长上下文、代码重构、疑难 bug 排查、架构设计等高复杂度任务 |

## 八、会话管理

### 上下文压缩

建议上下文占比达到 **60%** 时主动压缩。

| 好处 | 说明 |
| --- | --- |
| **省钱** | Token 越少，API 费用越低，长对话场景降幅尤为明显 |
| **避免报错** | 不会因超限导致调用失败，保障对话连续性 |
| **提升质量** | 保留关键信息、去除冗余噪音，让模型聚焦核心内容，避免"遗忘早期决策" |
| **降低延迟** | 压缩后上下文更小，推理速度更快，用户体验更流畅 |
| **支持长任务** | 使 AI 能在单次会话中完成需要几十甚至上百轮交互的复杂工程 |

- `/compact` — 主动压缩对话历史
- `/context` — 查看上下文占比、token 占比

### 状态栏配置

在对话框输入以下内容，让 CC 配置 statusLine，可随时在底部看到上下文占比：

> 帮我配一个 statusLine，能显示当前目录 + 模型 + 上下文剩余百分比

> [!NOTE] 配置后需重启 CC 生效

### 退出与恢复

- **退出 CC**：连按两次 `Ctrl + C`
- **恢复历史对话**：
  - `/resume` — 从历史对话列表中选择恢复
  - `claude -c` — 直接回到上一次对话（continue）

### 用 GitHub 保存项目状态

利用 GitHub 对 AI 辅助的项目进行历史状态保存，便于回溯和协作。

## 九、个性化配置

### 第一优先级：`CLAUDE.md` 文件

**全局级** — 所有项目通用的原则，位于 `C:\Users\Liuzwei\.claude`

> [!NOTE] 创建方式
> 1. 让 CC 自己写进全局 `CLAUDE.md`
> 2. 手动编辑 `C:\Users\Liuzwei\.claude\CLAUDE.md`

> [!NOTE] 内容建议
> - 不要塞入太多内容
> - 放最顶层、最稳定的长期执行内容
> - 逐步添加高频错误并修正

**项目级** — 单独的项目规则，位于项目的根目录下

> [!NOTE] 创建方式
> 1. 在工作目录下输入 `/init`，CC 会创建 `.claude` 文件夹和 `CLAUDE.md` 文件
> 2. 不用一开始就建立，可以等项目具备雏形之后再 `/init` 初始化

> [!NOTE] 内容建议
> - 跟随项目开发变化
> - 添加功能、更新要求
> - CC 踩过的坑同步更新到 CLAUDE.md

**子文件夹级**（可逐层递归）— 略

### 第二优先级：Auto-memory

`/memory` 手动打开 Agent 的 Auto-memory，主动记忆（记录在工作目录下的 `.claude` 文件中）。

### 第三优先级：自行构建文档

自己构建文档，在对话中作为上下文引用即可。

## 十、高级扩展

### 1. Skills — 操作手册

| 类型 | 说明 |
| --- | --- |
| 知识型 | 设计规范、语言规范等 |
| 流程型 | 工作流程指南 |
| 工具型 | 工具使用说明书 |
| 混合型 | 以上类型的组合 |

不是必用，但是常用。

> [!NOTE] 安装方式
> 1. 在 GitHub 上下载需要的 Skill 文件夹
> 2. 将整个 Skill 文件夹放置在全局或项目的 `.claude` 目录中
>
> ![[图片/Pasted image 20260510230019.png|600]]
>
> 必装两个 Skill：查找 skill 和创建 skill，用于构建个人 Skill 库

### 2. MCP

作为 AI 和外部应用的转接口。

### 3. CLI（Command Line Interface）

轻量化的 MCP。

### 4. SubAgent

**创建子Agent的两种方式：**

1. **自动触发：** 任务复杂且存在并行可能时，CC会自动派生子Agent并行推进
    
2. **手动创建：** 通过指令 `/agents` ，在Library界面进行创建

### 5. Hook

关键词：当 {XXXXX} 时，就要完成 {XXXXX}。

```Plain
设置一个hook，每次完成任务之后或者需要我做出选择时，都自动执行一个声音脚本，发出一个提示音"叮"进行提醒
```


方法：直接对 CC 说，让其自动配置。

### 6. Plugin

对以上 5 种扩展应用的打包。

## 十一、注意事项

- Claude Code 使用 CommonMark 规范渲染 Markdown
- 对话有上下文窗口限制，过长时系统会自动压缩早期消息
- 文件编辑使用精确匹配工具，缩进和空格需要保持一致
- Windows 环境下使用 Unix shell 语法（正斜杠路径等）


