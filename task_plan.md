# 标题层级统一计划

## 目标

将 `嵌入式/C&C++/` 下的 Markdown 笔记统一为：H1 使用中文数字、H2 使用 `1.`、H3 使用 `1.1`、H4 使用 `1.1.1`、H5 使用 `(1)`；没有 H1 的笔记补入文件名作为 H1。

## 阶段

1. [x] 读取标题规则、盘点目标笔记和现有工作区改动。
2. [x] 审核全部标题层级、frontmatter 和代码围栏边界。
3. [x] 更新项目规则与用户记忆。
4. [x] 批量统一标题并验证编号、层级与差异范围。

## 决策

- 不降低已有标题层级；缺少 H1 时在 frontmatter 后添加 `# 一、文件名`。
- 仅处理 Markdown 标题行，跳过 fenced code block。
- 保留当前工作区的未提交正文改动，不执行提交。
- 有多个 H1 的笔记，H2 编号在每个 H1 下重新开始；只有缺少 H1 的笔记新增文件名 H1。

## 错误记录

- `C:\\Users\\Liuzwei\\.skills-manager\\skills\\obsidian-titiled\\SKILL.md` 不存在；改用当前会话提供的可用标题整理 skill。
- 首次规则补丁未匹配当前 `AGENTS.md`；重新读取实际锚点后改用定点替换。
