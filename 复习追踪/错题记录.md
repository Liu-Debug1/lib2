# 错题记录

| 日期 | 题ID | 问题 | 用户答案 | 正确答案 | 解析 |
|------|------|------|---------|---------|------|
| 2026-05-21 | Q003 | 宏定义 `#define MUL(x,y) ((x)*(y))` 中多重括号的作用 | A. 提高执行效率 | B. 防止运算符优先级错误 | 宏是文本替换，`MUL(1+2,3)` 不加括号展开为 `1+2*3` 而非 `(1+2)*3` |
| 2026-05-21 | Q005 | `enum class` 相比普通 `enum` 的两个核心特性 | private/public 访问控制 | 限定作用域 + 类型安全校验 | `enum class` 枚举值必须通过 `枚举名::值` 访问，不同枚举类型间不能隐式比较 |
| 2026-05-21 | Q006 | `main.c` 文件包含哪两部分 | 初始化和 process() | 初始化和时序控制 | 第二空应为"时序控制"，不是具体函数名 |
| 2026-05-23 | Q005 | `enum class` 核心特性（再次错误） | 限定作用域（漏答第二空） | 限定作用域 + 类型安全校验 | 上次错后仍未记住第二特性——不同强枚举类型之间不能直接比较 |
| 2026-05-23 | Q021 | 浮点数不能直接用 `==` 比较的原因 | 不知道 | IEEE 754 舍入误差，用 `fabs(a-b) < epsilon` 容差比较 | 多数十进制小数无法精确转为二进制，运算累积误差，需用容差比较替代直接判等 |
| 2026-05-23 | Q022 | struct vs class 嵌入式选择策略 | public vs private 保护差异 | 需各举工程例子：struct→寄存器映射，class→设备驱动封装 | 原理正确但缺少具体工程实例支撑，简答题需达到 80% 语义完整度 |
| 2026-05-25 | Q034 | 类声明和类实例的命名方式 | 小驼峰 / 不知道 | 单段式大驼峰 / 两段式大驼峰 | 类声明如 `UserProfile` 是单段式大驼峰；类实例如 `RollerPlanner_C` 是两段式大驼峰+固定C后缀 |
| 2026-05-25 | Q036 | 指针级别转换：&变量→?、&一级指针→?、*一级指针→?、*二级指针→? | 一级 / 0级 / 二级 / 三级 | 一级 / 二级 / 变量 / 一级指针 | 核心规律：&升一级，*降一级。&一级指针得二级指针；*一级指针解引用得变量；*二级指针降级得一级指针 |
| 2026-05-25 | Q038 | this 指针的三个空 | 普通成员 / 本身这个类 / 不可修饰的类型 | 非静态 / 调用该函数的对象本身 / const ClassName* const | this 指向具体对象实例而非类；const 成员函数中 this 类型变为 const ClassName* const，双重 const 限制 |
| 2026-06-03 | Q082 | 链式调用值返回vs引用返回+const成员函数 | (1)方向对但不够精确；(2)误认为去掉const后const对象仍可调用print | (1)值返回导致链式操作作用于临时副本，原对象只增1次；(2)去掉const后const对象不能调用非const成员函数 | const 成员函数是 const 正确性的底层保障——const 对象只能调用 const 成员函数 |
| 2026-06-03 | Q090 | cin/cout 与 printf/scanf 特性对比 | 7空对5空；endl填"\r\n"而非"强制刷新缓冲区" | endl=强制刷新缓冲区；\n代替endl | endl = end line + flush，频繁IO时性能开销大；\n只换行不刷新 |
| 2026-06-03 | Q094 | 继承后子类对父类成员访问权限 | (2)误判能访问serialNumber | serialNumber是private成员，无论哪种继承方式子类都不能直接访问 | private 成员只属于基类自身，子类永远无法直接访问——这是封装的最底层保障 |
| 2026-06-11 | Q119 | 全局 `static` 变量能否被 `extern` 跨文件访问 | 正确，static使得变量成为全局静态变量 | 错误：全局 `static` 变量仅在当前 `.c` 文件内可见，不能通过 `extern` 跨文件访问 | 混淆了生命周期和作用域。`static` 让变量活得久，但没有放开文件可见性；跨文件共享应去掉 `static` |
